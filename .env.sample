# GCP Project (also used by gcloud CLI)
CLOUDSDK_CORE_PROJECT=your-gcp-project-id

# Workload Identity Federation
POOL_NAME=github-actions-pool
PROVIDER_NAME=github-actions-provider
SERVICE_ACCOUNT_NAME=newsletter-sa

# GitHub Repository (owner/repo format)
GITHUB_REPO=owner/repo-name

# Resend API
RESEND_API_KEY=re_xxxxxxxxxx
FROM_EMAIL=newsletter@yourdomain.com

# Claude Code Authentication (for newsletter generation)
# Choose ONE of the following based on your subscription:

# Option A: OAuth Token (for Claude Pro/Max subscribers)
# Obtain via: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxxxxxxxxx

# Option B: API Key (for API subscription users)
# Obtain from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxx

# Local development only (not needed for GitHub Actions)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
